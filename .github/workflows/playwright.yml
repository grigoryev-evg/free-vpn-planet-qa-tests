name: Playwright E2E

on:
  workflow_dispatch:
    inputs:
      suite:
        description: Test suite to run
        required: true
        default: smoke
        type: choice
        options:
          - all
          - smoke
          - known-issues

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: playwright-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: ubuntu-latest
    if: github.event.inputs.suite == 'all' || github.event.inputs.suite == 'smoke'

    outputs:
      pages_url: ${{ steps.deploy-pages.outputs.page_url }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run smoke suite
        run: npm run test:smoke

      - name: Upload smoke HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-smoke
          path: playwright-report/
          if-no-files-found: ignore

      - name: Upload smoke test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-smoke
          path: |
            test-results/
            artifacts/
          if-no-files-found: ignore

      - name: Setup GitHub Pages
        if: always()
        continue-on-error: true
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: playwright-report/

      - name: Deploy report to GitHub Pages
        id: deploy-pages
        if: always()
        continue-on-error: true
        uses: actions/deploy-pages@v4

      - name: Publish smoke summary
        if: always()
        shell: bash
        run: |
          {
            echo "## Smoke Report"
            echo
            echo "- Suite: \`smoke\`"
            echo "- HTML report artifact: \`playwright-report-smoke\`"
            echo "- Test results artifact: \`test-results-smoke\`"
            echo
            echo "Open the HTML report by downloading the \`playwright-report-smoke\` artifact and opening \`index.html\`."
            echo
            if [ -n "${{ steps.deploy-pages.outputs.page_url }}" ]; then
              echo "- GitHub Pages report: ${{ steps.deploy-pages.outputs.page_url }}"
            else
              echo "- GitHub Pages report: not available for this run"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  known-issues:
    runs-on: ubuntu-latest
    if: github.event.inputs.suite == 'all' || github.event.inputs.suite == 'known-issues'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run known issues suite
        run: npm run test:known-issues

      - name: Upload known issues HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-known-issues
          path: playwright-report/
          if-no-files-found: ignore

      - name: Upload known issues test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-known-issues
          path: |
            test-results/
            artifacts/
          if-no-files-found: ignore

      - name: Publish known issues summary
        if: always()
        shell: bash
        run: |
          {
            echo "## Known Issues Report"
            echo
            echo "- Suite: \`known-issues\`"
            echo "- HTML report artifact: \`playwright-report-known-issues\`"
            echo "- Test results artifact: \`test-results-known-issues\`"
            echo
            echo "These tests intentionally document current checkout defects and may fail by design."
          } >> "$GITHUB_STEP_SUMMARY"
